[build]
  command   = ""
  publish   = "."
  # 待ち時間データ(data/)の更新だけではデプロイを行わない。
  # スクレイパーは data/ を頻繁にコミットするが、サイト本体は静的なため
  # 再デプロイの必要がない。data/ 以外に差分が無ければ exit 0 でスキップされる。
  # （コミットメッセージの [skip ci] に加えた二重の保険）
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ':(exclude)data'"
